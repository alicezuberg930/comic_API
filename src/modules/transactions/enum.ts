export enum transactionTypes {
    "order payment", "wallet topup"
}

export enum transactionStatuses {
    "pending", "completed", "failed"
}

export enum paymentMethods {
    "momo", "vnpay", "zalopay", "onepay"
}